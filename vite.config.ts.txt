import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',
  },
  // Ensure VITE_ prefixes are allowed (default behavior but explicit is good)
  envPrefix: 'VITE_',
})